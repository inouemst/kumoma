# 雲間 HP構築 — 引継ぎ文書

> このファイルは、案内文生成パイプライン側のチャット（作業ディレクトリ
> `C:\Users\81908\Desktop\雲間`、リポジトリ`kumoma-pipeline`）から分離した、
> **サイトのデザイン・機能構築を専門に扱う新しいチャット**のための引継ぎ文書。
> 2026-08-06、バッチ16生成中に作成。

---

## 0. このディレクトリの正体

`C:\Users\81908\Desktop\kumoma-site` は、**GitHub Pagesが実際に配信している
公開リポジトリ`inouemst/kumoma`そのもののクローン**です。

- ここで`git push`すれば、1分ほどで公開サイト（`https://inouemst.github.io/kumoma/`）に反映される
- 「別に清書して転記する」ような中間ステップは不要。**このディレクトリ＝本番**

## 1. もう一つのチャット（案内文生成パイプライン）との役割分担

雲間には、この新しいチャットとは別に、**案内文の本番生成を担当する既存のチャット**が
`C:\Users\81908\Desktop\雲間`（リポジトリ`kumoma-pipeline`）で動いています。

- そちらは97件ずつ、17,456件中まだラベル付けされていない作品に「案内文」と4軸ラベルを生成し、
  `index.html`内の`const D=[...]`という巨大な配列（現在約6.4MB）にマージした上で、
  このリポジトリ（つまりこのディレクトリと同じ`inouemst/kumoma`）に直接pushしてきます
- **したがって、このディレクトリのファイルは、あなたが操作していない間にも
  別チャットによって書き換えられます。** セッションを始めるときは必ず`git pull`してください

### 触ってよいもの・触ってはいけないもの

| 対象 | 扱い |
|---|---|
| `index.html`のCSS・ヘッダー・ナビ・検索UI・レイアウト | **自由に触ってよい** |
| `about.html` / `otayori.html` / `whatsnew.html` | **自由に触ってよい** |
| `index.html`内の`const D=[...]`という配列そのもの | **触らない。** パイプライン側がマージする生成データ |
| `w[0]`〜`w[18]`という配列内インデックスの意味・並び順 | **勝手に変えない。** 変えるとパイプライン側のマージが壊れる |

`D`配列の各要素`w`の意味（インデックス）：
```
[0]id [1]題 [2]著者 [3]分 [4]文字遣い [5]文語 [6]漢字率 [7]会話率
[8]はじまり [9]省略 [10]入力 [11]校正 [12]底本 [13]著者dir [14]file [15]文字数
[16]案内文(無ければnull) [17]形式(無ければnull) [18]読後感(配列 or null)
```
新しい表示機能（絞り込み軸の追加など）でこの配列の中身を「読む」のは全く問題ありません。
書き換える・並び順を変える・新しいインデックスを追加する、といった操作だけを避けてください。
もし追加のインデックスが必要になったら、まずパイプライン側のチャットと調整が必要です
（`merge_batch.py`というマージスクリプトがこの並び順に依存しているため）。

`authors.json`・`index.json`は参照用の元データで、**サイトの表示には一切使われていません**
（`index.html`が読み込んでいるのは埋め込み済みの`D`配列のみ）。消す必要はありませんが、
「使われていないファイルがある」ことに戸惑わないように。

## 2. プロジェクトの大義（HPのデザイン判断すべての土台）

雲間は「読みやすい青空文庫ビューア」ではなく、**青空文庫という活動自体への入口**です。
収益化を目的とせず、青空文庫のボランティア文化（入力・校正の担い手）が
2039年の著作権保護期間延長明けまでやせ細らないことを支援するのが長期の狙いです。

デザイン判断で常に立ち返る原則：

- **上書きしない** — 雲間は読む場所ではない。本文の代わりを提供しない
- **序列をつけない** — 「傑作」「必読」等の評価語・人気順表示は使わない
- **寄生しない** — Wikipedia等を参照した説明文を書かない
- **書けないものは、書けないと言う** — 「準備中」ラベルを出さない、案内文がない作品も
  同じ視覚的重みで表示する（「育つ庭」モデル）
- `--aozora`（青）は**青空文庫へ出るリンク専用**。サイト内の機能リンク（お便り等）には使わない

## 3. 現在のサイト構成（2026-08-06時点）

| ページ | 役割 |
|---|---|
| `index.html` | ホーム。案内文つき作品からのランダム表示、複合軸検索（時間／読みやすさ／形式／読後感／キーワード）、一覧 |
| `whatsnew.html` | 新着文庫（青空文庫本家の新規公開作品、直近半年分）。`NEWS`配列・`AOZORA_STATS`定数とも手動更新 |
| `about.html` | 雲間について。原則、耕作員（ボランティア）参加案内、2039年問題の解説 |
| `otayori.html` | お便り。Googleフォームに接続済み（作品ID/作品名を事前入力） |

全ページ共通で、ヘッダー右上に「新着文庫／雲間について／お便り」の3リンクのナビ（`.topnav`）、
各ページ下部にも同内容のナビ（`.pagenav`）があります。現在地は`.here`クラスで強調表示。

### デザイントークン（全ページ共通、`:root`で定義）
```css
--sumi:#22262E;      /* 墨 — 地の文字色 */
--kumori:#F4F6F8;    /* 曇 — 背景 */
--kasumi:#DFE4EA;    /* 霞 — 罫線 */
--usu:#EDF0F4;       /* 淡霞 — 薄い塗り */
--ma:#7C8697;        /* 間 — 副次的な文字色 */
--aozora:#2C6E9B;    /* 青空 — 青空文庫へ出るリンク専用 */
--serif: "Hiragino Mincho ProN","Yu Mincho",YuMincho,"Noto Serif JP","Songti SC",serif; /* 本文・引用 */
--sans: "Hiragino Sans","Hiragino Kaku Gothic ProN","Yu Gothic",YuGothic,"Noto Sans JP",system-ui,sans-serif; /* UI */
```

### 手動更新が必要なデータ（自動化されていない）
- `whatsnew.html`の`NEWS`配列（青空文庫本家の新着一覧）と`AOZORA_STATS`定数
  （収録作品数・確認日時）は、どちらも手動で随時再取得・上書きする方式
- 目安として週1回程度の見直しが望ましい、と運営者から要望あり
  （パイプライン側のチャットが、セッション開始時に前回確認日時からの経過日数を
  チェックする運用にしている。このHP側のチャットで対応してもよい）

## 4. まだ手つかずの機能（RESEARCH_BRIEF.mdより、次の大きな差別化ポイント）

雲間の差別化は「発見（複合軸検索）」に一本化する方針が確定しています
（読書体験の提供はしない。0章の大義と矛盾するため不採用）。

- **隣から** — 埋め込み不要。ラベル（形式・主題・読後感・時代の質感）の重なりを
  ブラウザ内のJSで比較するだけで実装できる。追加費用ゼロ、着手しやすい
- **言葉から** — 真の意味検索（embedding）は検索のたびにAPI費用が発生するため不採用。
  代わりに、閉じたラベル語彙に対する「よくある言葉→ラベル」の対応表を静的ファイルとして
  事前に作成し埋め込む方式（疑似的な意味検索、検索時のAPI呼び出しなし）

この2つはまだ設計・実装ともに未着手です。着手する際は、このHPチャットが適任です
（パイプライン側は生成・検証・データマージに専念しているため）。

## 5. お便りフォームについて

`otayori.html`は稼働中のGoogleフォームに接続済みです。設定・事前入力の仕組みの詳細は
パイプライン側ディレクトリの`FORM_SETUP.md`（`C:\Users\81908\Desktop\雲間\FORM_SETUP.md`）を参照。
フォームの回答自体はGoogleフォームの「回答」タブに溜まる仕組みなので、
案内文への指摘が届いた場合の反映は、案内文データそのもの（`D`配列）に関わるため
パイプライン側チャットとの連携が必要になります。

## 6. 参考：既に確立している技術的な検証手順

新しいHTML/JSを書いたら、次の手順で壊れていないか確認する習慣があります（パイプライン側と共通）。

1. `<script>...</script>`の中身を抜き出して`.js`として保存し、`node --check`で構文確認
2. `html.parser`でタグの開閉バランスを確認
3. 内部リンク（`href="○○.html"`）が実在するページを指しているか確認
4. 公開後は`gh api repos/inouemst/kumoma/pages/builds/latest`でビルド状況を確認し、
   実際に`https://inouemst.github.io/kumoma/`へcurlしてみて変更が反映されているか確認する

---

以上が引継ぎ内容です。次のセッション開始時は、まず`git pull`で最新のバッチ反映を
取り込んでから作業を始めてください。
