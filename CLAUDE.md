# 雲間 HP — サイト構築プロジェクト

> このファイルはClaude Codeが毎セッション自動で読む指示書。
> 詳しい経緯・デザイントークン・役割分担の理由は`HP_HANDOVER.md`（同ディレクトリ）を参照。
> **セッション開始時は必ず最初に`HP_HANDOVER.md`を読むこと。**

## 30秒で分かること

- このディレクトリ＝公開リポジトリ`inouemst/kumoma`そのもの。`git push`で即座に本番反映
- **別のチャット**（`C:\Users\81908\Desktop\雲間`、`kumoma-pipeline`）が案内文の生成・マージを担当し、
  同じリポジトリに直接pushしてくる。**セッション開始時は必ず`git pull`すること**
- `index.html`内の`const D=[...]`配列は生成データ。**中身を読むのは自由、書き換え・構造変更は不可**
  （`w[0]`〜`w[18]`のインデックス順を変えるとパイプライン側のマージが壊れる）
- デザイン原則：上書きしない／序列をつけない／寄生しない／書けないものは書けないと言う。
  `--aozora`（青）は青空文庫への出口リンク専用
- まだ未着手の次の機能：「隣から」（ラベル重なりによる近傍表示）「言葉から」（静的な語→ラベル対応表）。
  詳細は`HP_HANDOVER.md`の4章

## 毎セッションの型

1. `git pull` — 別チャットの最新反映を取り込む
2. `HP_HANDOVER.md`を読み、現在のページ構成・デザイントークン・触ってよい範囲を確認
3. 作業
4. 新しいJS/HTMLは構文確認（`node --check`）とタグ開閉確認をしてからcommit・push
5. `gh api repos/inouemst/kumoma/pages/builds/latest`でビルド確認、実サイトで反映確認
